# AI Boot

Start here.

Read in this order:

1. `AI.md`
2. `.agents/BOOTSTRAP.md`

Hard rules:

- Follow bootstrap read order.
- Use `rtk` for shell.
- Keep exact AI `trade_plan` item JSON in `raw_json`.
- Do not reshape original `trade_plan` objects.
- Plan first for UI, feature, DB, or architecture changes unless user says execute now.
