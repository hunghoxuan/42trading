# Handoff — 2026-05-17

> From: Codex  
> To: Next agent / Self

## Deployed & Verified on VPS
- commit: `04726d0c`
- version: `v2026.05.17 16:14 - ca4da650`
- All code pushed to `origin/main`, VPS in sync

## What Was Done

### Source Tracking
- `SOURCE_STATUS` tracker in server with per-source connectivity/enabled/lastActivity
- `trackSourceActivity()` called in broker sync/pull/ack/heartbeat
- Bridge clients: `source_id="Ctrader"` / `source_id="MT5"` hardcoded in payloads
- Health page: per-source display with ● dot, Enabled/Disabled, time ago

### Cron Overhaul
- SNAPSHOTS_CRON: captures TradingView snapshots via Playwright, configurable in DB
- Independent per-cron execution with 120s timeout — one hanging won't block others
- `cronDetails` per cron in /health (marketData/aiAnalysis/snapshots)
- Cron tick notification every 60s via SYSTEM_EVENT
- BullMQ jobId colon fix (underscores)

### Bug Fixes
- **Direction flip**: Root-level `direction` was overriding plan-level — fixed priority (plan first)
- **Risk sizing**: `/v2/broker/pull` missing `risk_money`/`risk_pct` — bridges always used 1% default
- **updated_at**: Only updates on status changes, not PnL-only broker syncs
- **Broker sync notifications**: Only fire when execution_status changes (oldStatusMap)

### OpenRouter
- 11 models: GPT-4o, GPT-4.1, o3 Mini, Claude Sonnet 4, Claude 3.5, Gemini 2.5 Flash/Pro, DeepSeek V3/R1, Llama 4, Qwen3
- Model fallback + OpenRouter headers (HTTP-Referer, X-Title)

### UI
- Auto-reload on deploy (polls /health version)
- Dashboard filter labels on top
- ImageViewer component (click-preview-download-delete)
- Live chart fullscreen
- Master snapshot 410px + object-fit fill
- Font-size 10px for forms

## What Still Needs Work

### High Priority
- [ ] **SNAPSHOTS_CRON timeout**: Snapshots grid page times out (60s) — need faster rendering or increase timeout
- [ ] **cTrader source stays gray**: `CTRADER_MODE` env not set on VPS → source shows disabled. Set `CTRADER_MODE=demo`
- [ ] **UI build on VPS**: Build consistently times out over SSH. Need faster build or pre-built artifact deployment

### Medium Priority
- [ ] **Notification settings**: SYSTEM_EVENT should have toast enabled by default so cron notifications are visible
- [ ] **Trade plan editor**: `direction` field should be editable with BUY/SELL toggle
- [ ] **Risk display**: Show calculated risk amount in trade creation UI before sending

### Nice to Have
- [ ] **Per-source graphs**: Chart connectivity uptime over time on Health page
- [ ] **Cron schedule UI**: Let users configure cron cadence from Settings instead of DB
- [ ] **Snapshot gallery**: Browse all captured snapshots with date/time filters
- [ ] **Export/backup**: One-click DB backup from Health page
- [ ] **Mobile responsive**: Health page and Dashboard filters on small screens

## Key Files

| File | Purpose |
|------|---------|
| `webhook/server.js` | SOURCE_STATUS, cronDetails, SNAPSHOTS_CRON, risk fix, direction fix |
| `web-ui/src/pages/system/HealthPage.jsx` | Per-source + per-cron display |
| `web-ui/src/pages/ai/ChartSnapshotsPage.jsx` | Direction fix, OpenRouter models |
| `web-ui/src/components/ImageViewer.jsx` | Reusable image modal |
| `web-ui/src/components/charts/SymbolChart.jsx` | Fullscreen, snapshot click |
| `bridge-clients/TVBridge_CTrader.cs` | source_id="Ctrader" |
| `bridge-clients/TVBridgeEA.mq5` | source_id="MT5" |
